# This config was automatically generated from your source code
# Stacks detected: deps:php:.
version: 2.1
orbs:
  php: circleci/php@1
jobs:
  test-php:
    # Install php packages and run tests (PHP 8.4 matches composer.lock / doctrine-instantiator)
    docker:
      - image: cimg/php:8.4-node
    steps:
      - checkout
      - php/install-packages
      - run:
          name: run tests
          command: ./vendor/bin/phpunit
workflows:
  build-and-test:
    jobs:
      - test-php
